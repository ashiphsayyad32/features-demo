# Features Demo: Flask App

This is a simple Python Flask app for demonstrating the Cursor Bug Bot feature.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the app:
   ```bash
   python app.py
   ```

The app will be available at http://127.0.0.1:5000/ 